<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirect Warning</title>
    <style>
        body {
            background-color: whitesmoke;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
    </style>
</head>
<body>
    <div>
        <p>Hey!.. Don't be scared, we currently dont have a domain meaning you will be connecting to the sites direct IP, this is secure we assure you, press proceed if you understand, click off if you are uncomfortable.</p>
        <button id="proceedButton" disabled>Proceed</button>
    </div>

    <script>
        let countdown = <%= countdown %>;
        const proceedButton = document.getElementById('proceedButton');

        const updateButtonLabel = () => {
            if (countdown === 0) {
                proceedButton.textContent = 'Proceed';
                proceedButton.disabled = false;
                clearInterval(countdownInterval); // Clear the interval when countdown reaches 0
            } else {
                proceedButton.textContent = `Proceed in ${countdown} seconds`;
                proceedButton.disabled = true;
            }
        };

        const redirectToActualWebsite = () => {
            if (countdown === 0) {
                window.location.href = 'https://34.18.66.32/latest';
            }
        };

        updateButtonLabel();

        const countdownInterval = setInterval(() => {
            countdown--;
            updateButtonLabel();
        }, 1000);

        proceedButton.addEventListener('click', redirectToActualWebsite);
    </script>
</body>
</html>
